Description:
    This generator creates the migrations needed to use polymorphic paperclip

Example:
    ./script/generate polymorphic_paperclip

    This will create:
      - A migration, which adds a documents and attachings table.
